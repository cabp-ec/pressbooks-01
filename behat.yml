default:
    autoload:
        '': "%paths.base%/test/tests"
    suites:
        welcome:
            paths:    [ "%paths.base%/test/features" ]
            contexts: [ WelcomeContext ]
            filters:
                tags: "@welcome"
        search:
            paths:    [ "%paths.base%/test/features" ]
            contexts: [ SearchContext ]
            filters:
